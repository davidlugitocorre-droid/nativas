package com.example.ecomers.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Paleta de Colores de Lugo E-commerce (Material Design 3)
 * 
 * Diseñada para proyectar excelencia, confianza y elegancia premium.
 * Combina un Indigo sofisticado como tono principal, un Ámbar cálido para botones y acciones,
 * y fondos suaves que optimizan la lectura y reducen el cansancio visual.
 */
val Primary = Color(0xFF4F46E5)          // Indigo 600 (Elegancia y Estabilidad)
val OnPrimary = Color(0xFFFFFFFF)        // Blanco Puro
val PrimaryContainer = Color(0xFFEEF2FF) // Indigo muy suave para fondos de cards secundarias
val OnPrimaryContainer = Color(0xFF312E81)

val Secondary = Color(0xFFF59E0B)        // Ámbar Dorado (Acciones clave, carrito, checkout)
val OnSecondary = Color(0xFFFFFFFF)
val SecondaryContainer = Color(0xFFFEF3C7) // Ámbar suave
val OnSecondaryContainer = Color(0xFF78350F)

val Tertiary = Color(0xFF06B6D4)         // Cyan (Metadatos, etiquetas y badges)
val OnTertiary = Color(0xFFFFFFFF)
val TertiaryContainer = Color(0xFFECFEFF)
val OnTertiaryContainer = Color(0xFF164E63)

val Error = Color(0xFFEF4444)            // Rojo Brillante (Advertencias y Errores)
val OnError = Color(0xFFFFFFFF)
val ErrorContainer = Color(0xFFFEE2E2)
val OnErrorContainer = Color(0xFF7F1D1D)

val Background = Color(0xFFF9FAFB)       // Slate 50 (Fondo General de la App)
val OnBackground = Color(0xFF111827)     // Slate 900 (Texto Principal)

val Surface = Color(0xFFFFFFFF)          // Blanco (Contenedores de tarjetas, listas y inputs)
val OnSurface = Color(0xFF111827)
val SurfaceVariant = Color(0xFFF3F4F6)   // Gris Claro para separadores y hover
val OnSurfaceVariant = Color(0xFF4B5563)

val Outline = Color(0xFFD1D5DB)          // Gris Borde (Inputs y contornos)
val OutlineVariant = Color(0xFFE5E7EB)

// Gradientes Estéticos Premium para Dashboards y Encabezados
val MainGradientStart = Color(0xFF6366F1)
val MainGradientEnd = Color(0xFF4F46E5)
val GoldGradientStart = Color(0xFFFBBF24)
val GoldGradientEnd = Color(0xFFF59E0B)
